class Main{
    function void main(){
        var Fraction a,b,c;
        let a=Fraction.new(2,4);
        let b=Fraction.new(1,6);
        let c=a.plus(b);
        do c.print();
        return ;

    }
}